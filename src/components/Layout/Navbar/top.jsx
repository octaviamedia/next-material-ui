import { AppBar, Typography, Toolbar } from "@material-ui/core";

const TopNav = () => {
	return <AppBar static>
		<Toolbar>
			<Typography variant="h6">Octavia</Typography>
			<div>

			</div>
		</Toolbar>
	</AppBar>;
};

export default TopNav;